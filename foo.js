/* 
	 all problems must use some combination of forEach, map, reduce or filter
*/

// ----------------------------
//   Q.1 
//
// Assuming that x_0 is a 4-D vector, use one of the functions to compute the magnitude
var x_0 = [ 0.91,  4.31,  2.10,  0.07];
// => return as a scalar


// ----------------------------
//   Q.2 
//
// Assuming that x is an array of row of 4-D vector, compute the magnitude vector of each row.
// => return as an array with 12 elements
var x = [
   [ 0.91,  4.31,  2.10,  0.07],
   [-1.80, -5.88, -6.16, -0.79],
   [ 1.47, -0.59, -1.06, -5.25],
   [ 2.22, -3.62, -2.47, -0.86],
   [ 5.14,  8.72, -4.73, -2.49],
   [-0.58,  2.48,  1.52, -2.94],
   [-6.42,  4.14,  0.85, -3.47],
   [-2.52, -3.17,  0.10, -1.60],
   [ 4.06, -1.41, -4.00, -6.01],
   [-3.22, -0.82,  3.38,  2.89],
   [ 2.88,  3.30,  1.05,  1.56],
   [ 0.37, -0.83, -0.90, -0.06]
]

// ----------------------------
//   Q.3 
//
// Assuming that x is an array of row of 4-D vector, produce an output array that only contains 
//  vectors whose magnitude is greater than 8.0
// => return as an array with N elements, each Nth element is a size 4 array

// ----------------------------
//   Q.4 
//
// Assuming that x is an array of row of 4-D vector, produce an output array that only contains 
//  vectors whose magnitude is within the bounds of an object.
//   Run twice using the following objects:
//		 {'low' : 4.5, 'high' : 8.0 }
//		 {'low' : 5.0, 'high' : 6.0 }
// => return as an array with N elements, each Nth element is a size 4 array

// ----------------------------
//   Q.5 
//
// Assuming that x is an array of row of 4-D vector, produce an object that contains the magnitude 
// of the maximum and minimum as keyed values. The output should look something like:
// { max : 12.7 , min : 0.04 }

// ----------------------------
//   Q.6 
//
// Assuming that x is an array of row of 4-D vector, produce an object that contains the magnitude 
// of the largest and smallest vectors as keyed values. The output should look something like:
// { max : 12.7 , min : 0.04 }

// ----------------------------
//   Q.7 
//
// Assuming that x is an array of row of 4-D vector, produce an object that contains the magnitude 
// of the largest and smallest vectors as keyed values. The output should look something like:
// { max : 12.7 , min : 0.04 }

